# super-clipboard 隐私说明

super-clipboard 默认记录全部剪贴板内容，包括文本、HTML、图片和文件列表。这是当前产品决策，目的是保证历史记录完整。

## 本地存储

历史记录保存在本机应用数据目录下的 SQLite 数据库中。图片和大二进制内容保存为本地 blob 文件，数据库保存引用路径。

## 用户控制

- 可以暂停记录。
- 可以删除单条记录。
- 可以清空历史。
- 可以设置最大历史条数和最长保存时间。

## 不包含

首版不包含云同步、远程上传、多设备同步或 OCR。应用不应主动把剪贴板内容发送到网络服务。
